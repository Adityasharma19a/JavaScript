"use strict"; //By this it will treate the whole Js code as newer version

// object

// Type off
let age = 90


// When we see the type of null it is object
console.log(typeof null);
// Whereas undefined's type is undefined
console.log(typeof undefined)